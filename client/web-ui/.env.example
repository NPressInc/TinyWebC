# Backend host configuration
# For local development (everything on same machine):
# REACT_APP_BACKEND_HOST=localhost

# For SSH/remote backend (backend on remote server):
# REACT_APP_BACKEND_HOST=your-remote-server-ip-or-hostname
# Example: REACT_APP_BACKEND_HOST=192.168.1.100
# Example: REACT_APP_BACKEND_HOST=myserver.example.com
